    
    let maximo = 30;
    let aleatorio = Math.random() * maximo;

    let aleatorioFormateado = Math.floor(aleatorio)
    //console.log(aleatorio)
    console.log(aleatorioFormateado)

    // valorAleatorio = Math.floor(Math.random() * temperaturaMaxima);

    // sensor.lecturas.push(valorAleatorio)
